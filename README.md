# Documentation for Iranians

Things are segregated by branches in case this grows. The idea being that a single branch can be pulled instead of the entire repo.Binary files are stored using git-lfs. I'm not familiar with the internals of git-lfs, hopefully that functionality isn't blocked.

## Godot 4.6 stable offline docs
branch: godot

